/**
 * The ControllerInterface interface defines the methods that must be implemented by
 * a controller in the MVC architecture for interacting with the user interface and model.
 */
public interface ControllerInterface {
  /**
   * Starts the controller's functionality, typically by setting up event listeners
   * and initializing the user interface.
   */
  void start();
}
